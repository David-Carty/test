index.html has all of its own assets in the app folder.
the app/index.scss contains all of the css that the user wishes to add or use to overwrite 
the css that is preloaded at startup.

If the user wishes to override any component or view settings then the overrides are done in the respective component or view files.