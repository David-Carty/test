This is mowzi's cdn.  It contains the runtime and all of the JS for the UI.
It will be run from Wasabi.
Each time a user makes a request it will show in their analytics as data usage and they will
be billed for it.